-- This file is used instead of the original create_index.sql because
-- it is not stable with OrioleDB. After create_index.sql stabilization this one
-- must be replaced with original. Currently it contains only indexes for alter_table.sql
-- script.
CREATE INDEX onek_unique1 ON onek USING btree(unique1 int4_ops);
CREATE INDEX tenk1_unique1 ON tenk1 USING btree(unique1 int4_ops);
